package org.johnny.processing.transformations

import org.apache.spark.sql.DataFrame
import org.johnny.processing.DataProcessor

class DataCleaner extends DataProcessor {
  override def process(input: DataFrame): DataFrame = {
    null
  }
}
